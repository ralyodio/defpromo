{
  "name": "defpromo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "BROWSER=chrome vite build && BROWSER=chrome node scripts/post-build.js && cd dist/chrome && zip -r chrome.zip .",
    "build:chrome": "BROWSER=chrome vite build && BROWSER=chrome node scripts/post-build.js && cd dist/chrome && zip -r chrome.zip .",
    "build:firefox": "BROWSER=firefox vite build && BROWSER=firefox node scripts/post-build.js && cd dist/firefox && zip -r firefox.zip .",
    "build:safari": "BROWSER=safari vite build && BROWSER=safari node scripts/post-build.js && cd dist/safari && zip -r safari.zip .",
    "build:all": "pnpm build:chrome && pnpm build:firefox && pnpm build:safari",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "lint": "eslint src --ext js,jsx",
    "format": "prettier --write \"src/**/*.{js,jsx,css}\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ralyodio/defpromo.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "bugs": {
    "url": "https://github.com/ralyodio/defpromo/issues"
  },
  "homepage": "https://github.com/ralyodio/defpromo#readme",
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.16",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.0",
    "@vitejs/plugin-react": "^5.1.0",
    "@vitest/ui": "^4.0.3",
    "autoprefixer": "^10.4.21",
    "eslint": "^9.38.0",
    "eslint-config-prettier": "^10.1.8",
    "fake-indexeddb": "^6.2.4",
    "jsdom": "^27.0.1",
    "postcss": "^8.5.6",
    "prettier": "^3.6.2",
    "tailwindcss": "^4.1.16",
    "vite": "^7.1.12",
    "vitest": "^4.0.3"
  },
  "dependencies": {
    "dexie": "^4.2.1",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "recharts": "^3.3.0"
  }
}
